org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Google Mobile Ads and UMP are AndroidX libraries. The SDKs are included only
# in the Play flavor, but AndroidX must be enabled for that variant to resolve.
android.useAndroidX=true
android.nonTransitiveRClass=true
